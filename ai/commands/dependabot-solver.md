---
description: Fetch and resolve Dependabot security vulnerabilities
---

Help the user resolve Dependabot security alerts.

First, invoke the skill tool to load the dependabot-solver skill:

```
skill({ name: 'dependabot-solver' })
```

Then follow the skill instructions to help the user resolve their Dependabot security alerts
